name: My First Action

on:
  push:        # Runs every time you push a commit
    branches:
      - main

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "🎉 Hello from GitHub Actions!"
